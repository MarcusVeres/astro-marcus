---
const navigationItems = [
  { name: 'Insights', href: '/blog' },
  { name: 'Resources', href: '/about' },
  { name: 'Services', href: '/tags' },
  { name: 'Shop', href: '/tags' },
];
---

<el-popover-group class="hidden lg:flex lg:gap-x-12">
  {navigationItems.map(item => (
    <a href={item.href} class="text-xs font-bold tracking-widest text-white uppercase transition-all duration-300 ease-in-out hover:text-yellow-300">
      {item.name}
    </a>
  ))}
</el-popover-group>